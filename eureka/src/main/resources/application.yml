eureka:
  client:
    service-url:
      defaultZone: ${hostname:localhost}/abc
    register-with-eureka: false
  server:
    enable-self-preservation: false
spring:
  application:
    name: xoxoEureka
server:
  port: 8761